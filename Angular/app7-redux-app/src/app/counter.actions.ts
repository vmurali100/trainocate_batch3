import { createAction } from '@ngrx/store';

export const increment = createAction('[My Counter Component] Increment')
export const decrement = createAction('[My Counter Component] Decrement')
export const reset = createAction('[My Counter Component] Reset')

